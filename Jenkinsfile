@Library('pipeline-library-demo')_

pipeline {
    agent any
    stages {
        stage('Demo') {
            steps {
                printHeader 'Chapter I'
                echo 'Hello from the master branch.'
            }
        }
    }
}
